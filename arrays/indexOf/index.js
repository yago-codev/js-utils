const arr = ['🍎', '🍐', '🍊', '🍌']

// a função indexOf será utilizada para buscarmos o índice
// de um determinado elemento:
const indiceLaranja = arr.indexOf('🍊')

console.log(indiceLaranja) // 2

const indiceBanana = arr.indexOf('🍌')

console.log(indiceBanana) // 3
